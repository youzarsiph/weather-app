import { StyleSheet } from 'react-native'

const ScreenStyles = StyleSheet.create({
  centeredScreen: {
    flex: 1,
    gap: 16,
    alignItems: 'center',
    justifyContent: 'center',
  },
})

export { ScreenStyles }
